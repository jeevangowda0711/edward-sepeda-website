name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: [self-hosted, edward-website]
    steps:
      - uses: actions/checkout@v4

      - name: Copy files to web root
        run: rsync -av --delete --exclude='.git' --exclude='.github' . /var/www/edward-sepeda/
